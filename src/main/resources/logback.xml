<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-5level] %logger{15} - %msg%n%rEx</pattern>
        </encoder>
        <immediateFlush>false</immediateFlush>
    </appender>

    <!-- Set to DEBUG only while diagnosing HTTP failures: failed-session dumps can contain secrets. -->
    <logger name="io.gatling.http.engine.response" level="WARN" />

    <!-- Scenario / request builders (INFO logs stay below root if root is ERROR) -->
    <logger name="builders" level="INFO"/>
    <logger name="core" level="INFO"/>

    <!-- root level "ERROR" or "INFO" -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>
